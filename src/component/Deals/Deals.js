import React from 'react';
import NavBar from '../NavBar/NavBar';

const Deals = () => {
    return (
        <div>
            <NavBar />
            <h1> Check your Deals!</h1>
        </div>
    );
};

export default Deals;